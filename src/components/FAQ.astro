---
interface Props { items: readonly { question: string; answer: string }[] }
const { items } = Astro.props;
---
<div class="faq-list">{items.map(item => <details><summary>{item.question}<span aria-hidden="true">+</span></summary><p>{item.answer}</p></details>)}</div>
